(()=>{'use strict';
const P=new URLSearchParams(location.search),id=P.get('id')||'g3-l01';
const R={'g3-l01':{ar:'التعريف والتهيئة',fr:'Introduction',folder:'01_Introduction',interactive:'01_interactif_introduction.html',exam:'04_examen_introduction.html'},'g3-l02':{ar:'في الفصل',fr:'En classe',folder:'02_En_classe',interactive:'01_interactif_enclasse.html',exam:'04_examen_enclasse.html'},'g3-l03':{ar:'الأفعال والأنشطة',fr:'Actes et verbes',folder:'03_Actes_et_verbes',interactive:'01_interactif_actesverbes.html',exam:'04_examen_actesverbes.html'},'g3-l04':{ar:'هذا فصلي',fr:'C’est ma classe',folder:'04_Unite1_Cest_ma_classe',interactive:'01_interactif_maclasse.html',exam:'04_examen_maclasse.html'},'g3-l05':{ar:'هذا منزلي',fr:'C’est ma maison',folder:'05_Unite2_Cest_ma_maison',interactive:'01_interactif_maison.html',exam:'04_examen_maison.html'},'g3-l06':{ar:'الفواكه والخضروات',fr:'Fruits et légumes',folder:'06_Unite3_Fruits_et_legumes',interactive:'01_interactif_fruits.html',exam:'04_examen_fruits.html'}};
if(!R[id]){document.body.innerHTML='<main style="padding:24px;font-family:Arial"><h2>الدرس غير متاح</h2><p>معرف الدرس غير صالح.</p></main>';return}
const d=R[id],root='https://raw.githubusercontent.com/hm-french-academy/hm-french-academy/main/data/lessons/grade-3/web/3eme%20prim/'+d.folder+'/',$=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const sections=[['start','🚀 البداية'],['vocab','🖼️ المفردات'],['pron','🎧 النطق'],['grammar','📘 القواعد'],['conversation','💬 المحادثة'],['practice','✍️ التدريب'],['media','🎬 الفيديو'],['games','🎮 مركز الألعاب'],['assessment','🏆 التقييم'],['progress','📈 التقدم']];
let doc=null,sourceBlocks=[];
async function load(){const r=await fetch(root+d.interactive+'?v=20260822-final',{cache:'no-store'});if(!r.ok)throw Error('تعذر تحميل المصدر الأصلي للدرس ('+r.status+')');const html=await r.text();doc=new DOMParser().parseFromString(html,'text/html');doc.querySelectorAll('script,style,noscript,svg,iframe').forEach(e=>e.remove());sourceBlocks=[...doc.querySelectorAll('section.block')];if(!sourceBlocks.length)sourceBlocks=[...doc.querySelectorAll('.block')];if(!sourceBlocks.length)sourceBlocks=[...doc.body.children].filter(e=>(e.textContent||'').trim().length>20);}
function cleanClone(el){const c=el.cloneNode(true);c.querySelectorAll('script,style,noscript,svg,iframe').forEach(e=>e.remove());c.querySelectorAll('[id]').forEach(e=>e.removeAttribute('id'));c.querySelectorAll('[onclick]').forEach(e=>e.removeAttribute('onclick'));c.querySelectorAll('[style]').forEach(e=>e.removeAttribute('style'));c.querySelectorAll('[hidden],[aria-hidden="true"]').forEach(e=>{e.removeAttribute('hidden');e.removeAttribute('aria-hidden')});c.querySelectorAll('input').forEach(e=>{if(e.type==='hidden')e.remove()});return c}
function tagOf(b){return (b.querySelector('.block-head .tag,.tag')?.textContent||'').replace(/\s+/g,' ').trim().toLowerCase()}
function textOf(b){return (b.textContent||'').replace(/\s+/g,' ').trim()}
function groups(){const out={vocab:[],grammar:[],conversation:[],practice:[],media:[],pron:[]};sourceBlocks.forEach(b=>{const t=tagOf(b)+' '+textOf(b).slice(0,900);if(/vocab|lexique|mot|vocabulaire/.test(t))out.vocab.push(b);if(/grammaire|grammar|règle|conjug|verbe/.test(t))out.grammar.push(b);if(/dialogue|conversation|communiquer|parler|situation/.test(t))out.conversation.push(b);if(/exercice|activité|entraînement|practice|jeu|complète|associe|relie|vrai|faux/.test(t))out.practice.push(b);if(/pronon|phon|écoute|audio|son|répète|repeat/.test(t))out.pron.push(b);if(/vidéo|video|multimédia|chanson/.test(t)||b.querySelector('video,audio,.video-frame,[data-video]'))out.media.push(b)});return out}
function renderBlocks(arr,title){const V=$('#viewer');const use=arr.length?arr:sourceBlocks;if(!use.length){V.innerHTML='<section class="card"><h2>'+title+'</h2><p class="empty">لا توجد بيانات لهذا القسم في المصدر الأصلي لهذا الدرس.</p></section>';return}V.innerHTML='<section class="card"><h2>'+title+'</h2><p class="source-note">محتوى مستخرج من المصدر الأصلي للدرس الحالي.</p></section>'+use.map(b=>{const c=cleanClone(b);return '<section class="card source-section">'+c.innerHTML+'</section>'}).join('');wireSpeech();}
function start(){const first=sourceBlocks.slice(0,2).map(cleanClone).map(x=>x.innerHTML).join('');$('#viewer').innerHTML='<section class="card"><h2>🚀 البداية</h2>'+first+'</section>';}
function media(){const urls=[...doc.querySelectorAll('video,audio,source,a[href*="youtube"],a[href*="youtu.be"]')].map(e=>e.currentSrc||e.src||e.href||e.getAttribute('src')).filter(Boolean);const unique=[...new Set(urls)];$('#viewer').innerHTML='<section class="card"><h2>🎬 الفيديو والوسائط</h2>'+(unique.length?unique.map(u=>/\.(mp4|webm)(\?|$)/i.test(u)?'<video controls playsinline src="'+esc(u)+'"></video>':/\.(mp3|wav|ogg)(\?|$)/i.test(u)?'<audio controls src="'+esc(u)+'"></audio>':'<a class="primary" target="_blank" rel="noopener" href="'+esc(u)+'">فتح الوسيط ↗</a>').join(''):'<p class="empty">لا توجد وسائط مستقلة في المصدر الأصلي لهذا الدرس.</p>')+'</section>'}
function vocabWords(){const g=groups(),nodes=g.vocab.length?g.vocab:sourceBlocks;const vals=nodes.flatMap(b=>[...b.querySelectorAll('.stamp,.flashcard,.word,.fr,.term,button')].map(e=>(e.textContent||'').replace(/\s+/g,' ').trim()).filter(x=>x.length>1&&x.length<80));return [...new Set(vals)].slice(0,24)}
function games(){const vs=vocabWords();$('#viewer').innerHTML='<section class="card"><h2>🎮 مركز الألعاب</h2><p>الألعاب مبنية على مفردات هذا الدرس فقط.</p><div class="gamebox"><strong id="gw">'+esc(vs[0]||'لا توجد مفردات مستخرجة')+'</strong><div id="gc"></div></div></section>';if(!vs.length)return;let i=0;function round(){const a=vs[i%vs.length],opts=[a,...vs.filter(x=>x!==a).sort(()=>Math.random()-.5).slice(0,3)];$('#gw').textContent=a;$('#gc').innerHTML=opts.map(x=>'<button data-a="'+esc(x)+'">'+esc(x)+'</button>').join('');$('#gc').querySelectorAll('button').forEach(b=>b.onclick=()=>{b.textContent=b.dataset.a===a?'✅ صحيح':'❌ حاول مرة أخرى';if(b.dataset.a===a){i++;setTimeout(round,450)}})}round()}
function assessment(){$('#viewer').innerHTML='<section class="card"><h2>🏆 التقييم</h2><p>التقييم الإلكتروني الخاص بهذا الدرس فقط.</p><a class="primary" target="_blank" rel="noopener" href="'+root+d.exam+'">فتح التقييم ↗</a></section>'}
function progress(){const k='hm:g3:journey:'+id,done=localStorage.getItem(k)==='1';$('#viewer').innerHTML='<section class="card"><h2>📈 التقدم</h2><p>'+esc(d.fr)+'</p><button id="done">'+(done?'✅ مكتمل':'تسجيل إكمال الدرس')+'</button></section>';$('#done').onclick=()=>{localStorage.setItem(k,'1');progress()}}
function wireSpeech(){document.querySelectorAll('#viewer [data-say]').forEach(b=>b.onclick=()=>{const u=new SpeechSynthesisUtterance(b.dataset.say||b.textContent);u.lang='fr-FR';u.rate=.85;speechSynthesis.cancel();speechSynthesis.speak(u)})}
function show(sec){const g=groups();document.querySelectorAll('[data-sec]').forEach(b=>b.classList.toggle('active',b.dataset.sec===sec));if(sec==='start')start();else if(sec==='vocab')renderBlocks(g.vocab,'🖼️ المفردات');else if(sec==='pron')renderBlocks(g.pron.length?g.pron:g.vocab,'🎧 النطق');else if(sec==='grammar')renderBlocks(g.grammar,'📘 القواعد');else if(sec==='conversation')renderBlocks(g.conversation,'💬 المحادثة');else if(sec==='practice')renderBlocks(g.practice,'✍️ التدريب');else if(sec==='media')media();else if(sec==='games')games();else if(sec==='assessment')assessment();else progress()}
(async()=>{try{await load();$('#title').textContent=d.ar+' · '+d.fr;$('#subtitle').textContent='رحلة الدرس';$('#tabs').innerHTML=sections.map(x=>'<button class="tab" data-sec="'+x[0]+'">'+x[1]+'</button>').join('');document.querySelectorAll('[data-sec]').forEach(b=>b.onclick=()=>show(b.dataset.sec));show((location.hash||'#start').slice(1))}catch(e){$('#viewer').innerHTML='<section class="card"><h2>تعذر تحميل محتوى الدرس</h2><p>'+esc(e.message)+'</p></section>'}})();})();