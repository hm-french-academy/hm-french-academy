(()=>{'use strict';
const params=new URLSearchParams(location.search),id=params.get('id')||'g3-l01';
const pages=[['lesson','البداية'],['discover','اكتشف'],['vocabulary','المفردات'],['grammar','القاعدة'],['conversation','المحادثة'],['practice','حان دورك'],['pronunciation','اسمع واختر'],['media','الوسائط'],['games','مركز الألعاب'],['assessment','التقييم'],['postLesson','بعد الرحلة']];
let d=null,cur=0;
const $=s=>document.querySelector(s),viewer=$('#viewer'),rail=$('#journey');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=v=>String(v??'').trim().toLowerCase().replace(/[؟?!.،,]/g,'').replace(/\s+/g,' ');
function speak(t){try{speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(t);u.lang='fr-FR';u.rate=.84;speechSynthesis.speak(u)}catch{}}
function dataStore(){try{return JSON.parse(localStorage.getItem('hm:g3premium:'+id)||'{}')}catch{return{}}}
function save(v){try{localStorage.setItem('hm:g3premium:'+id,JSON.stringify({...dataStore(),...v}))}catch{}}
function railRender(){rail.innerHTML=pages.map((p,i)=>`<button class="step ${i===cur?'active':''}" data-page="${i}"><span class="stepIcon">${i+1}</span>${p[1]}</button>`).join('')}
function nav(){return `<div class="booknav"><button class="g3btn" data-prev ${cur===0?'disabled':''}>السابق</button><span class="g3pill">${cur+1} / ${pages.length}</span><button class="g3btn" data-next ${cur===pages.length-1?'disabled':''}>التالي</button></div>`}
function shell(k,title,text,body=''){viewer.innerHTML=`<div class="g3panel"><small>${esc(k)}</small><h2>${title}</h2><p>${esc(text)}</p></div>${body}${nav()}`;railRender()}
function qCard(x,label){return `<article class="g3card qcard" data-q="${esc(x.id)}"><span class="g3pill">${label}</span><div class="qprompt" dir="ltr">${esc(x.prompt||'')}</div>${x.audio?`<button class="g3btn listen" data-text="${esc(x.audio)}">🔊 استمع</button>`:''}<div class="g3choices">${(x.options||[]).map(o=>`<button class="g3btn ans" data-answer="${esc(o)}">${esc(o)}</button>`).join('')}</div><div class="feedback"></div></article>`}
function wireQuestions(items,root=document){root.querySelectorAll('.qcard').forEach(card=>{const x=items.find(a=>String(a.id)===card.dataset.q);if(!x)return;card.querySelectorAll('.ans').forEach(btn=>btn.onclick=()=>{const ok=(x.acceptedAnswers||[]).some(a=>norm(a)===norm(btn.dataset.answer));card.querySelector('.feedback').textContent=ok?'🎉 إجابة صحيحة!':'💡 حاول مرة أخرى.';btn.classList.toggle('selected',ok);if(ok){const c={...dataStore().completed};c[x.id]=true;save({completed:c})}})});root.querySelectorAll('.listen').forEach(b=>b.onclick=e=>{e.stopPropagation();speak(b.dataset.text)})}
function lesson(){shell('01 · البداية',`🚀 ${esc(d.title)}`,'ابدأ من هنا. كل فتح للدرس يبدأ من الصفحة الأولى.',`<div class="g3cards"><article class="g3card"><span class="g3pill">🎯 الهدف</span><h3>تعارف وتواصل</h3><p>الاسم، الحال، العمر، ثم استخدام العبارات في حوار قصير.</p></article><article class="g3card"><span class="g3pill">📖 طريقة الكتاب</span><p>تعلّم → جرّب → استمع → العب → قيّم نفسك.</p></article></div>`)}
function discover(){const e=d.expressions||[];shell('02 · اكتشف','🔎 اكتشف','الصيغ والبدائل كما وردت في المادة الأصلية.',`<div class="g3cards">${e.map(x=>`<article class="g3card"><span class="g3pill">${esc(x.category)}</span><h3 dir="ltr">${esc((x.questions||[]).join(' / '))}</h3>${(x.answers||[]).map(a=>`<button class="g3btn listen" data-text="${esc(a)}" dir="ltr" style="width:100%;margin:5px 0;text-align:left">${esc(a)} 🔊</button>`).join('')}</article>`).join('')}</div>`)}
function vocabulary(){const a=d.vocabulary||[];shell('03 · المفردات','🧩 المفردات','اضغط على العبارة أو زر الصوت لسماع النطق.',`<div class="g3cards">${a.map(x=>`<article class="g3card"><div dir="ltr" class="wordFr">${esc(x.fr)}</div><div>${esc(x.ar)}</div><button class="g3btn listen" data-text="${esc(x.audio||x.fr)}">🔊 اسمع</button></article>`).join('')}</div>`)}
function grammar(){const a=d.grammar||[];shell('04 · القاعدة','🧠 القاعدة','نحافظ على صيغ السؤال والإجابات والبدائل الموجودة في المصدر.',`<div class="g3cards">${a.map(x=>`<article class="g3card"><span class="g3pill">${esc(x.label)}</span><p>${esc(x.note)}</p>${(x.questionForms||[]).map(q=>`<button class="g3btn listen" data-text="${esc(q)}" dir="ltr" style="width:100%;margin:4px 0;text-align:left">${esc(q)} 🔊</button>`).join('')}<hr>${(x.answers||[]).map(q=>`<button class="g3btn listen" data-text="${esc(q)}" dir="ltr" style="width:100%;margin:4px 0;text-align:left">${esc(q)} 🔊</button>`).join('')}</article>`).join('')}</div>`)}
function conversation(){const a=d.conversation||[];shell('05 · المحادثة','💬 المحادثة','استمع إلى الحوار ثم كرر العبارات.',`<div class="g3cards">${a.map(x=>`<article class="g3card"><span class="g3pill">${esc(x.speaker)}</span><button class="g3btn listen" data-text="${esc(x.audio||x.text)}" dir="ltr" style="width:100%;text-align:left">${esc(x.text)} 🔊</button></article>`).join('')}</div>`)}
function practice(){const a=d.practice||[];shell('06 · حان دورك','🎯 حان دورك','سؤال الحال يقبل الإجابات الأربع الواردة في المصدر.',`<div class="g3cards">${a.map(x=>qCard(x,'✍️ تدريب')).join('')}</div>`);wireQuestions(a)}
function pronunciation(){const a=d.listening||[];shell('07 · اسمع واختر','🎧 اسمع واختر','استمع إلى السؤال أو العبارة ثم اختر معناها الصحيح.',`<div class="g3cards">${a.map(x=>qCard(x,'🎧 استمع')).join('')}</div>`);wireQuestions(a)}
function media(){const m=d.media||{};shell('08 · الوسائط','🎬 الوسائط','الوسائط الأصلية داخل الكتاب.',m.youtubeId?`<div class="g3cards"><article class="g3card"><h3>${esc(m.title||'وسائط الدرس')}</h3><div style="aspect-ratio:16/9"><iframe src="https://www.youtube.com/embed/${encodeURIComponent(m.youtubeId)}" style="width:100%;height:100%;border:0;border-radius:16px" allowfullscreen></iframe></div></article></div>`:`<div class="g3card"><p>لا توجد وسائط مضمّنة لهذا الدرس.</p></div>`)}
function runGame(c,g){const body=c.querySelector('.gamebody');if(!g)return;if(g.type==='sentence-builder'){const t=g.target||[],w=[...t].sort(()=>Math.random()-.5);body.innerHTML=`<div class="g3choices">${w.map(x=>`<button class="g3btn word" dir="ltr">${esc(x)}</button>`).join('')}</div><button class="g3btn check">تحقق</button><div class="feedback"></div>`;let chosen=[];body.querySelectorAll('.word').forEach(b=>b.onclick=()=>{chosen.push(b.textContent);b.disabled=true;body.querySelector('.feedback').textContent=chosen.join(' ')});body.querySelector('.check').onclick=()=>{const ok=chosen.join(' ')===t.join(' ');body.querySelector('.feedback').textContent=ok?(g.successMessage||'🎉 ممتاز!'):(g.retryMessage||'💡 حاول مرة أخرى.');if(ok)speak(g.audio||t.join(' '))};return}if(g.type==='listen-choice'){const a=(d.listening||[]).filter(x=>(g.items||[]).includes(x.id));body.innerHTML=a.map(x=>qCard(x,'🎧')).join('');wireQuestions(a,body);return}if(g.id==='mood-match'){const x=(d.expressions||[]).find(e=>e.id==='ask-mood');body.innerHTML=`<p>صيغتا السؤال: <b dir="ltr">${esc((x?.questions||[]).join(' / '))}</b></p><div class="g3cards">${(x?.answers||[]).map(a=>`<button class="g3btn listen" data-text="${esc(a)}" dir="ltr">${esc(a)} 🔊</button>`).join('')}</div><div class="feedback">الإجابات الأربع مقبولة مع سؤال الحال.</div>`;body.querySelectorAll('.listen').forEach(b=>b.onclick=()=>speak(b.dataset.text));return}const a=(d.vocabulary||[]).slice(0,8);body.innerHTML=`<div class="g3cards">${a.map(x=>`<article class="g3card"><b dir="ltr">${esc(x.fr)}</b><p>${esc(x.ar)}</p><button class="g3btn listen" data-text="${esc(x.fr)}">🔊</button></article>`).join('')}</div>`;body.querySelectorAll('.listen').forEach(b=>b.onclick=()=>speak(b.dataset.text))}
function games(){const gs=d.games||[];shell('09 · مركز الألعاب','🎮 مركز الألعاب','اختر لعبة لتبدأ؛ كل لعبة تعمل داخل الكتاب.',`<div class="g3cards">${gs.map((g,i)=>`<article class="g3card game"><span class="g3pill">لعبة ${i+1}</span><h3>${esc(g.title)}</h3><p>${esc(g.description)}</p><button class="g3btn play">▶ ابدأ اللعبة</button><div class="gamebody"></div></article>`).join('')}</div>`);viewer.querySelectorAll('.play').forEach((b,i)=>b.onclick=()=>runGame(b.closest('.game'),gs[i]))}
function assessment(){const a=d.assessment||[];shell('10 · التقييم','🏆 التقييم','اختر إجابة صحيحة. سؤال الحال يقبل الإجابات الأربع.',`<div class="g3cards">${a.map(x=>qCard(x,'🏆 سؤال')).join('')}</div><button class="g3btn" id="scoreBtn">احسب النتيجة</button><div id="score" class="feedback"></div>`);wireQuestions(a);$('#scoreBtn').onclick=()=>{const done=dataStore().completed||{};const score=a.filter(x=>done[x.id]).length;save({assessmentScore:score,assessmentTotal:a.length});$('#score').textContent=`نتيجتك: ${score} / ${a.length}`}}
function modal(title,body){const m=document.createElement('div');m.className='studioModal';m.innerHTML=`<div class="modalCard"><h2>${title}</h2>${body}<br><button class="g3btn close">عودة إلى الكتاب</button></div>`;document.body.appendChild(m);m.querySelector('.close').onclick=()=>m.remove();return m}
function reference(){const r=d.reference||{};modal('📘 '+(r.title||'مرجع الدرس'),`<div class="g3cards">${(r.items||[]).map(x=>`<article class="g3card">${esc(x)}</article>`).join('')}</div><p>المصدر: ${esc(r.source||'')}</p>`)}
function official(){const a=(d.assessment||[]).map((x,i)=>({...x,id:'official-'+i}));const m=modal('📝 التقييم الرسمي',`<p>أجب عن أسئلة التقييم داخل المنصة.</p><div class="g3cards">${a.map(x=>qCard(x,'السؤال')).join('')}</div>`);wireQuestions(a,m)}
function smart(){const all=[...(d.practice||[]),...(d.assessment||[])],done=dataStore().completed||{},weak=all.filter(x=>!done[x.id]);const m=modal('🧠 المراجعة الذكية',`<p>${weak.length?'هذه العناصر تحتاج تدريبًا إضافيًا:':'🎉 ممتاز! لا توجد عناصر غير مكتملة.'}</p><div class="g3cards">${weak.map(x=>qCard(x,'مراجعة')).join('')}</div>`);wireQuestions(weak,m)}
function postLesson(){shell('11 · بعد الرحلة','🎓 بعد الرحلة','أكملت رحلة الدرس. اختر أداة للمتابعة.',`<div class="g3cards"><article class="g3card"><h3>📘 مرجع الدرس</h3><p>راجع الكلمات والعبارات والقواعد.</p><button class="g3btn" data-tool="reference">فتح المرجع</button></article><article class="g3card"><h3>📝 التقييم الرسمي</h3><p>اختبار تفاعلي داخل المنصة.</p><button class="g3btn" data-tool="official">فتح التقييم</button></article><article class="g3card"><h3>🧠 المراجعة الذكية</h3><p>مراجعة العناصر التي تحتاج إلى تدريب.</p><button class="g3btn" data-tool="smart">ابدأ المراجعة</button></article></div>`)}
const renderers=[lesson,discover,vocabulary,grammar,conversation,practice,pronunciation,media,games,assessment,postLesson];
function render(i){cur=Math.max(0,Math.min(pages.length-1,i));renderers[cur]();window.scrollTo({top:0,behavior:'smooth'})}
rail.addEventListener('click',e=>{const b=e.target.closest('[data-page]');if(b)render(Number(b.dataset.page))});
viewer.addEventListener('click',e=>{const next=e.target.closest('[data-next]'),prev=e.target.closest('[data-prev]'),tool=e.target.closest('[data-tool]');if(next&&!next.disabled)render(cur+1);if(prev&&!prev.disabled)render(cur-1);if(tool){const t=tool.dataset.tool;({reference,official,smart}[t]||(()=>{}))()}});
async function boot(){try{const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw new Error('تعذر تحميل بيانات الدرس');d=await r.json();cur=0;$('#title').textContent=d.title||id;$('#subtitle').textContent='كتاب تفاعلي Premium · يبدأ من الصفحة الأولى';render(0)}catch(err){viewer.innerHTML=`<div class="g3panel"><h2>تعذر فتح الدرس</h2><p>${esc(id)} · ${esc(err.message)}</p><button class="g3btn" onclick="location.reload()">إعادة المحاولة</button></div>`}}
boot();
})();