// HM Academy shared media loader — student-facing language only.
function hmYouTubeId(url){try{const u=new URL(url);if(u.hostname==='youtu.be')return u.pathname.slice(1);if(u.hostname.includes('youtube.com'))return u.searchParams.get('v')||u.pathname.split('/').filter(Boolean).pop();}catch(e){}return null;}
function hmStudentMediaMessage(kind){const messages={unavailable:'المحتوى المرئي والمسموع لهذا الدرس قيد التجهيز.',empty:'سنضيف موارد تعليمية جديدة لهذا الدرس قريبًا.',error:'حدثت مشكلة بسيطة أثناء تحميل المحتوى. يمكنك المحاولة مرة أخرى.'};return messages[kind]||messages.error}
async function loadLessonMedia(lessonId,containerId='lesson-media'){const container=document.getElementById(containerId);if(!container)return;try{const response=await fetch('data/media.json');if(!response.ok){container.innerHTML='<p>'+hmStudentMediaMessage('unavailable')+'</p>';return}const data=await response.json();const items=(data.media||[]).filter(item=>item.lessonId===lessonId);if(!items.length){container.innerHTML='<p>'+hmStudentMediaMessage('empty')+'</p>';return}container.innerHTML=items.map(item=>{const source=String(item.source||'');if(item.type==='audio')return `<div class="media-card"><h3>🔊 ${item.title||'استماع'}</h3><audio controls preload="metadata"><source src="${source}"></audio><p>${item.description||''}</p></div>`;if(item.type==='video'){const id=hmYouTubeId(source);if(id)return `<div class="media-card"><h3>🎥 ${item.title||'فيديو تعليمي'}</h3><div style="position:relative;padding-top:56.25%;border-radius:18px;overflow:hidden;background:#101827"><iframe src="https://www.youtube.com/embed/${encodeURIComponent(id)}" title="${item.title||'فيديو تعليمي'}" loading="lazy" allow="accelerometer;autoplay;clipboard-write;encrypted-media;gyroscope;picture-in-picture;web-share" allowfullscreen style="position:absolute;inset:0;width:100%;height:100%;border:0"></iframe></div><p>${item.description||''}</p></div>`;return `<div class="media-card"><h3>🎥 ${item.title||'فيديو تعليمي'}</h3><video controls preload="metadata" width="100%"><source src="${source}"></video><p>${item.description||''}</p></div>`}return ''}).join('')}catch(error){console.warn('HM media load failed',error);container.innerHTML='<p>'+hmStudentMediaMessage('error')+'</p>'}}
window.HMYouTubeId=hmYouTubeId;
