(()=>{'use strict';window.HMGrade3SmartReviewL02={mount(root,d){const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const norm=x=>String(x??'').trim().toLowerCase().replace(/[؟?!.,]/g,'').replace(/\s+/g,' ');const qs=[...d.practice.map(q=>({title:'تدريب ذكي',prompt:q.prompt,options:q.options,answers:q.acceptedAnswers})),...d.listening.map(q=>({title:'استماع',prompt:q.prompt,audio:q.audio,options:q.options,answers:q.acceptedAnswers})),...d.assessment.map(q=>({title:'تقييم سريع',prompt:q.prompt,options:q.options,answers:q.acceptedAnswers}))];let n=0,score=0;const speak=x=>{if(!x||!speechSynthesis)return;speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(x);u.lang='fr-FR';u.rate=.82;speechSynthesis.speak(u)};function render(){if(n>=qs.length){root.innerHTML='<div class="sr-head"><span>🎉</span><div><h2>انتهت المراجعة الذكية</h2><p>النتيجة: '+score+'/'+qs.length+'</p></div></div>';return}const q=qs[n];root.innerHTML='<div class="sr-head"><span>🧠</span><div><small>سؤال '+(n+1)+' من '+qs.length+'</small><h2>'+esc(q.title)+'</h2></div></div><div class="g3card"><h3>'+esc(q.prompt)+'</h3>'+(q.audio?'<button type="button" class="g3btn" id="audio">🔊 استمع</button>':'')+'<div class="g3choices">'+q.options.map((o,i)=>'<button type="button" class="g3btn answer" data-i="'+i+'">'+esc(o)+'</button>').join('')+'</div><div class="feedback"></div><button type="button" class="g3btn" id="check">تحقق</button></div>';let selected=null;root.querySelectorAll('.answer').forEach(b=>b.onclick=()=>{root.querySelectorAll('.answer').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');selected=b.dataset.i});root.querySelector('#audio')?.addEventListener('click',()=>speak(q.audio));root.querySelector('#check').onclick=()=>{const chosen=selected===null?'':q.options[+selected];const ok=q.answers.some(a=>norm(a)===norm(chosen));root.querySelector('.feedback').textContent=ok?'✅ صحيح':'💡 راجع إجابتك';if(ok){score++;root.querySelector('#check').disabled=true;setTimeout(()=>{n++;render()},700)}}}render()}}})();