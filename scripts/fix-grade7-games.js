const fs=require('fs');
const path='grade7-lesson-studio.html';
let s=fs.readFileSync(path,'utf8');
const old="if(currentSection==='games')return `<div class=\"card enrich\"><h2>🎮 مركز الألعاب</h2>${list(jc.games||cfg.games?.games)}</div>`;";
if(!s.includes(old)) throw new Error('Grade 7 games branch not found');
const helper=`function renderGames(){if(id!=='grade7-u1-l1')return '<div class="card enrich"><h2>🎮 مركز الألعاب</h2><div class="notice">الألعاب التفاعلية لهذا الدرس ستظهر هنا بعد ربطها ببياناته المعتمدة.</div></div>';const items=(data?.journeyContent?.vocabulary?.items||[]).filter(Array.isArray).map(x=>({fr:String(x[0]||''),ar:String(x[2]||x[1]||'')})).filter(x=>x.fr);const letters='ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('');let game='race';const shuffle=a=>a.slice().sort(()=>Math.random()-.5);const state={score:0,round:0};function vocabPool(){return items.filter(x=>!letters.includes(x.fr));}function makeRound(){state.round++;if(game==='race'){const answer=letters[Math.floor(Math.random()*letters.length)],opts=shuffle([answer,...shuffle(letters.filter(x=>x!==answer)).slice(0,2)]);return {title:'🏁 سباق الحروف',prompt:'اختر الحرف المطلوب:',answer,opts};}if(game==='who'){const pool=vocabPool();const target=pool[Math.floor(Math.random()*pool.length)]||{fr:'Un taxi',ar:'تاكسي'};const opts=shuffle([target,...shuffle(pool.filter(x=>x.fr!==target.fr)).slice(0,2)]).map(x=>x.fr);return {title:'🕵️ من أنا؟',prompt:'أنا كلمة فرنسية معناها: '+target.ar,answer:target.fr,opts};}const pool=vocabPool();const trio=shuffle(pool).slice(0,3);const answer=trio.map(x=>x.fr).sort((a,b)=>a.localeCompare(b,'fr'))[0]||'Un taxi';return {title:'🔤 رتّب الكلمات',prompt:'أي كلمة تأتي أولًا أبجديًا؟',answer,opts:shuffle(trio.map(x=>x.fr))};}function draw(){const r=makeRound();panelEl.innerHTML='<div class="card enrich"><div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px"><button class="btn" data-g="race">🏁 سباق الحروف</button><button class="btn" data-g="who">🕵️ من أنا؟</button><button class="btn" data-g="order">🔤 رتّب الكلمات</button></div><h2>'+r.title+'</h2><p>'+esc(r.prompt)+'</p><div class="grid">'+r.opts.map(o=>'<button type="button" class="card game-option" data-answer="'+esc(o)+'" style="cursor:pointer;text-align:center;font-weight:900">'+esc(o)+'</button>').join('')+'</div><div id="gameResult" class="small" style="margin-top:10px">الجولة '+state.round+' — النتيجة '+state.score+'</div></div>';panelEl.querySelectorAll('[data-g]').forEach(b=>b.onclick=()=>{game=b.dataset.g;state.score=0;state.round=0;draw()});panelEl.querySelectorAll('[data-answer]').forEach(b=>b.onclick=()=>{const ok=b.dataset.answer===r.answer;panelEl.querySelectorAll('[data-answer]').forEach(x=>x.disabled=true);b.classList.add(ok?'ok':'notice');if(ok)state.score++;document.getElementById('gameResult').innerHTML=(ok?'✅ إجابة صحيحة':'❌ إجابة غير صحيحة — الصحيح: <b>'+esc(r.answer)+'</b>')+'<br>النتيجة: '+state.score+' / '+state.round+' <button class="btn" type="button" id="nextGame">التالي</button>';document.getElementById('nextGame').onclick=draw})}return (draw(),panelEl.innerHTML);}`;
s=s.replace(old,"if(currentSection==='games')return renderGames();");
s=s.replace('function render(){',helper+'function render(){');
fs.writeFileSync(path,s);
console.log('Grade 7 interactive games patch applied');
