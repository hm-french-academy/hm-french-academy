(()=>{'use strict';
/* Grade 5 build-game validity layer: accept pedagogically valid S+V+C combinations,
   not only one memorized sentence. It intercepts only the Build tab before v10. */
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const P=new URLSearchParams(location.search),ID=P.get('id')||'g5-t1-l01';let D=null;
const shuffle=a=>{const x=[...a];for(let i=x.length-1;i>0;i--)[[x[i],x[Math.floor(Math.random()*(i+1))]]=[x[Math.floor(Math.random()*(i+1))],x[i]]];return x};
const clean=s=>String(s??'').trim().replace(/\s+/g,' ').replace(/[.!?]+$/,'');
async function load(){for(const u of ['data/lessons/grade-5/lesson-studio-content.json','data/lessons/grade-5/full-course-content.json']){try{const r=await fetch(u+'?v=20260820-111',{cache:'no-store'});let x=await r.json();for(let i=0;i<8&&x;i++){if(typeof x==='string'){try{x=JSON.parse(x);continue}catch{break}}if(typeof x.content==='string'){try{x=JSON.parse(x.content);continue}catch{break}}break}if(x?.lessons?.[ID]){D=x.lessons[ID];return}}catch{}}}
function pool(){const a=(D?.grammar?.examples||[]).filter(x=>x?.fr),out=[],seen=new Set();for(const x of a){const parts=(x.parts?.length?x.parts:clean(x.fr).split(' ')).map(clean);if(parts.length<3)continue;const k=parts.join('|').toLowerCase();if(!seen.has(k)){seen.add(k);out.push({parts,fr:parts.join(' ')})}}if(ID==='g5-t1-l01')[['Je','regarde','un film'],['Je','parle','anglais'],['Tu','aimes','le français'],['Il','regarde','un film'],['Elle','écoute','la musique'],['Nous','aimons','le sport'],['Vous','parlez','français'],['Ils','regardent','un film'],['Elles','écoutent','la musique'],['Mona','parle','français'],['Ali','aime','le français'],['Sara','lit','un livre'],['Paul','aime','le sport'],['Nora','écoute','la musique'],['Adam','regarde','un film']].forEach(parts=>{const k=parts.join('|').toLowerCase();if(!seen.has(k)){seen.add(k);out.push({parts,fr:parts.join(' ')})}});return out}
function css(){if($('#g5v11'))return;const s=document.createElement('style');s.id='g5v11';s.textContent=`.g5v11-label{font-weight:900;margin-top:10px}.g5v11-bank{display:flex;flex-wrap:wrap;gap:9px;justify-content:center;margin:10px 0}.g5v11-bank button{border:1px solid #475569;background:#20252d;color:#fff;border-radius:13px;padding:11px 15px;font-weight:900;cursor:pointer}.g5v11-bank button:disabled{opacity:.35}.g5v11-zone{min-height:58px;border:2px dashed #64748b;border-radius:16px;padding:10px;display:flex;flex-wrap:wrap;gap:8px;align-items:center;justify-content:center;background:#111827;margin:12px 0}.g5v11-zone span{font-weight:900}.g5v11-actions{display:flex;gap:9px;flex-wrap:wrap}.g5v11-actions button{border:0;border-radius:11px;padding:10px 15px;font-weight:900;cursor:pointer}.g5v11-check{background:#2563eb;color:#fff}.g5v11-reset{background:#334155;color:#fff}.g5v11-next{background:#d51b78;color:#fff}.g5v11-fb{min-height:30px;margin-top:10px;font-weight:900;line-height:1.7}.g5v11-rule{padding:12px 14px;border-radius:14px;background:#ffffff10;border:1px solid #ffffff22;line-height:1.8;margin-bottom:12px}`;document.head.appendChild(s)}
function render(){const data=pool();if(data.length<3)return;const subjects=[...new Set(data.map(x=>x.parts[0]))],pairs=new Map();data.forEach(x=>{if(!pairs.has(x.parts[0]))pairs.set(x.parts[0],new Set());pairs.get(x.parts[0]).add(x.parts[1])});const verbs=[...new Set(data.map(x=>x.parts[1]))],comps=[...new Set(data.map(x=>x.parts.slice(2).join(' ')))],questions=shuffle(data).slice(0,Math.min(10,data.length)),total=questions.length,p=$('#gpanel');let n=0,score=0;
 const paint=()=>{const target=questions[n];$('#gbar').style.width=(n/total*100)+'%';const sub=shuffle(subjects).slice(0,5);if(!sub.includes(target.parts[0]))sub.push(target.parts[0]);const initialSubject=target.parts[0];p.innerHTML=`<div class="g5v10-q">${n+1}/${total} · بناء الجملة</div><div class="g5v11-rule"><b>كوّن أي جملة فرنسية صحيحة.</b><br>اختر الفاعل، ثم فعلًا يتوافق معه، ثم مكمّلًا مناسبًا. لا توجد جملة واحدة محفوظة.</div><div class="g5v11-label">1) Sujet · الفاعل</div><div class="g5v11-bank" id="bs"></div><div class="g5v11-label">2) Verbe · الفعل</div><div class="g5v11-bank" id="bv"></div><div class="g5v11-label">3) Complément · المكمّل</div><div class="g5v11-bank" id="bc"></div><div class="g5v11-zone" id="bb">اختر الأجزاء الثلاثة…</div><div class="g5v11-actions"><button class="g5v11-check" type="button">تحقق</button><button class="g5v11-reset" type="button">إعادة</button></div><div class="g5v11-fb"></div>`;const state=[null,null,null],renderBuilt=()=>{$('#bb').innerHTML=state.every(Boolean)?state.map(x=>`<span>${esc(x)}</span>`).join(' + '):'اختر الأجزاء الثلاثة…'};
 const drawSubjects=()=>{$('#bs').innerHTML=sub.map(x=>`<button type="button" data-v="${esc(x)}">${esc(x)}</button>`).join('');$('#bs').querySelectorAll('button').forEach(b=>b.onclick=()=>{state[0]=b.dataset.v;state[1]=null;$('#bs').querySelectorAll('button').forEach(z=>z.disabled=z===b);drawVerbs();drawComps();renderBuilt()})};
 const drawVerbs=()=>{const chosenSub=state[0]||initialSubject,valid=[...(pairs.get(chosenSub)||[])];let arr=shuffle(valid).slice(0,4);if(!arr.length)arr=[target.parts[1]];if(!arr.includes(target.parts[1])&&valid.includes(target.parts[1]))arr.push(target.parts[1]);$('#bv').innerHTML=arr.map(x=>`<button type="button" data-v="${esc(x)}">${esc(x)}</button>`).join('');$('#bv').querySelectorAll('button').forEach(b=>b.onclick=()=>{state[1]=b.dataset.v;$('#bv').querySelectorAll('button').forEach(z=>z.disabled=z===b);renderBuilt()})};
 const drawComps=()=>{let arr=shuffle(comps).slice(0,5);if(!arr.includes(target.parts.slice(2).join(' ')))arr.push(target.parts.slice(2).join(' '));$('#bc').innerHTML=arr.map(x=>`<button type="button" data-v="${esc(x)}">${esc(x)}</button>`).join('');$('#bc').querySelectorAll('button').forEach(b=>b.onclick=()=>{state[2]=b.dataset.v;$('#bc').querySelectorAll('button').forEach(z=>z.disabled=z===b);renderBuilt()})};drawSubjects();drawVerbs();drawComps();
 p.querySelector('.g5v11-reset').onclick=paint;p.querySelector('.g5v11-check').onclick=()=>{if(p.dataset.done)return;p.dataset.done='1';const subject=state[0],verb=state[1],comp=state[2],ok=!!subject&&!!verb&&!!comp&&pairs.get(subject)?.has(verb)&&comps.includes(comp);p.querySelector('.g5v11-fb').innerHTML=ok?`✅ جملة صحيحة — أحسنت!<br><b>${esc([subject,verb,comp].join(' '))}</b>`:'❌ هذه التركيبة غير صحيحة. اختر فعلًا متوافقًا مع الفاعل ومكمّلًا مناسبًا.';if(ok)score++;const next=document.createElement('button');next.className='g5v11-next';next.type='button';next.textContent=n+1<total?'التالي ←':'إنهاء اللعبة';next.onclick=()=>{if(n+1<total){n++;delete p.dataset.done;paint()}else{p.innerHTML=`<h2>🎉 انتهت اللعبة</h2><p>نتيجتك: <b>${score}/${total}</b></p><button class="primary-btn" id="v11again" type="button">إعادة اللعبة</button>`;$('#gbar').style.width='100%';$('#v11again').onclick=()=>{n=0;score=0;paint()}}};p.querySelector('.g5v11-actions').appendChild(next)};paint()}
function intercept(e){const b=e.target.closest?.('.g5v10-tab');if(!b||b.dataset.g!=='build')return;if(!$('#gpanel'))return;e.preventDefault();e.stopImmediatePropagation();e.stopPropagation();css();render()}
(async()=>{css();await load();document.addEventListener('click',intercept,true)})();
})();