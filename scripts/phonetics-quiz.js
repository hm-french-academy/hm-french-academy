(function(){'use strict';function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function render(host,quiz,lessonId){if(!host||!quiz?.length)return;host.innerHTML='<div class="ph-quiz"><h3>اختبار الدرس</h3>'+quiz.map((q,i)=>'<fieldset data-q="'+i+'"><legend>'+esc(q.q)+'</legend>'+q.options.map((o,j)=>'<label><input type="radio" name="q'+i+'" value="'+j+'"> '+esc(o)+'</label>').join('')+'</fieldset>').join('')+'<button type="button" id="phSubmit">تصحيح الاختبار</button><div id="phResult"></div></div>';host.querySelector('#phSubmit').onclick=function(){let score=0;quiz.forEach((q,i)=>{const a=host.querySelector('input[name=q'+i+']:checked');const f=host.querySelector('[data-q="'+i+'"]');f.classList.remove('correct','wrong');if(a&&Number(a.value)===q.answer){score++;f.classList.add('correct')}else f.classList.add('wrong')});const pct=Math.round(score/quiz.length*100);host.querySelector('#phResult').textContent='النتيجة: '+score+'/'+quiz.length+' — '+pct+'%';if(window.HMPhoneticsProgress)window.HMPhoneticsProgress.complete(lessonId,pct)}}window.HMPhoneticsQuiz={render}})();