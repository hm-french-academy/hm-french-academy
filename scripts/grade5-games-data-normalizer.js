(()=>{'use strict';const original=window.fetch;window.fetch=async function(input,init){const url=typeof input==='string'?input:input?.url||'';if(!url.includes('data/lessons/grade-5/lesson-studio-content.json'))return original.apply(this,arguments);const r=await original.apply(this,arguments);const raw=await r.clone().text();let x;try{x=JSON.parse(raw)}catch{return r}for(let i=0;i<8;i++){if(typeof x==='string'){try{x=JSON.parse(x)}catch{break}}if(x&&typeof x.content==='string'){try{x=JSON.parse(x.content);continue}catch{}}break}const flat=x?.lessons?x:(x?.content?.lessons?x.content:x);return new Response(JSON.stringify(flat),{status:r.status,statusText:r.statusText,headers:{'Content-Type':'application/json'}})}})();