const userHandler = require('../api/handlers/userHandler');
const courseHandler = require('../api/handlers/courseHandler');
const lessonHandler = require('../api/handlers/lessonHandler');
const assessmentHandler = require('../api/handlers/assessmentHandler');
const progressHandler = require('../api/handlers/progressHandler');

module.exports = {
  userHandler,
  courseHandler,
  lessonHandler,
  assessmentHandler,
  progressHandler
};
